{
  "name": "lint-uz-suite",
  "version": "2.0.0",
  "description": "Suite of client-side format viewers under lint.one",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy:site": "node build.mjs site && cd site && npx wrangler deploy",
    "deploy:json": "node build.mjs json && cd json && npx wrangler deploy",
    "deploy:xml": "node build.mjs xml && cd xml && npx wrangler deploy",
    "deploy:yaml": "node build.mjs yaml && cd yaml && npx wrangler deploy",
    "deploy:csv": "node build.mjs csv && cd csv && npx wrangler deploy",
    "dev:site": "node build.mjs site && cd site && npx wrangler dev",
    "dev:json": "node build.mjs json && cd json && npx wrangler dev",
    "dev:xml": "node build.mjs xml && cd xml && npx wrangler dev",
    "dev:yaml": "node build.mjs yaml && cd yaml && npx wrangler dev",
    "dev:csv": "node build.mjs csv && cd csv && npx wrangler dev",
    "deploy:pdf": "node build.mjs pdf && cd pdf && npx wrangler deploy",
    "dev:pdf": "node build.mjs pdf && cd pdf && npx wrangler dev",
    "deploy:log": "node build.mjs log && cd log && npx wrangler deploy",
    "dev:log": "node build.mjs log && cd log && npx wrangler dev",
    "deploy:redirect": "cd redirect && npx wrangler deploy",
    "waitlist": "cd site && npx wrangler d1 execute lintone-waitlist --remote --command=\"SELECT format, COUNT(*) AS people FROM interest GROUP BY format ORDER BY people DESC\""
  },
  "keywords": [
    "json",
    "xml",
    "yaml",
    "csv",
    "linter",
    "viewer",
    "formatter",
    "pdf",
    "log"
  ],
  "author": "saidkamolxon",
  "license": "MIT"
}
